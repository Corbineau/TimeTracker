//include the database link here

//need to have the on-click event

$("#").on("click", () => {
    //get the new employee values from the input fields
    //store them in the database
    //append new info to the employee table
});